-- Platoboost Key System (Zysume UI remake, fixed editable key box)

-- CONFIG
local service = 9661
local secret = "33c4eb06-e149-43fd-a328-b042251cccc4"
local useNonce = true

local HttpService = game:GetService("HttpService")
local Players = game:GetService("Players")
local player = Players.LocalPlayer
local StarterGui = game:GetService("StarterGui")

local function onMessage(msg)
	StarterGui:SetCore("ChatMakeSystemMessage", { Text = "[Platoboost] " .. msg })
end

local fRequest = request or http_request or syn.request
local fSetClipboard = setclipboard or toclipboard
local fGetHwid = gethwid or function() return tostring(player.UserId) end

local function lEncode(tbl) return HttpService:JSONEncode(tbl) end
local function lDecode(str) return HttpService:JSONDecode(str) end
local function lDigest(input)
	local out = ""
	for i = 1, #input do out ..= string.format("%02x", string.byte(input, i)) end
	return out
end

local host = "https://api.platoboost.app"
local ok, res = pcall(function() return fRequest({ Url = host .. "/public/connectivity", Method = "GET" }) end)
if not ok or not res or (res.StatusCode ~= 200 and res.StatusCode ~= 429) then
	host = "https://api.platoboost.net"
end

local function verifyKey(key)
	local url = string.format("%s/public/whitelist/%s?identifier=%s&key=%s", host, service, lDigest(fGetHwid()), key)
	local res = fRequest({ Url = url, Method = "GET" })
	if not res then return false, "No response." end
	local data = lDecode(res.Body)
	if data.success and data.data.valid then
		return true, "Key verified!"
	else
		return false, "Invalid key."
	end
end

local function copyLink()
	local res = fRequest({
		Url = host .. "/public/start",
		Method = "POST",
		Headers = { ["Content-Type"] = "application/json" },
		Body = lEncode({
			service = service,
			identifier = lDigest(fGetHwid()),
		}),
	})
	if res and res.StatusCode == 200 then
		local data = lDecode(res.Body)
		if data.success and data.data.url then
			if fSetClipboard then
				fSetClipboard(data.data.url)
			end
			return true, "Copied link! Paste it in your browser."
		end
	end
	return false, "Failed to copy link."
end

-- == GUI ==
task.spawn(function()
	local gui = Instance.new("ScreenGui")
	gui.Name = "PlatoboostKeySystem"
	gui.ResetOnSpawn = false
	gui.Parent = player:WaitForChild("PlayerGui")

	local frame = Instance.new("Frame")
	frame.Size = UDim2.new(0, 360, 0, 200)
	frame.Position = UDim2.new(0.5, -180, 0.5, -100)
	frame.BackgroundColor3 = Color3.fromRGB(10, 10, 10)
	frame.BorderColor3 = Color3.fromRGB(255, 255, 255)
	frame.BorderSizePixel = 2
	frame.Parent = gui

	local title = Instance.new("TextLabel")
	title.Text = "ZuriScripts Key System"
	title.Size = UDim2.new(1, -30, 0, 30)
	title.Position = UDim2.new(0, 15, 0, 5)
	title.BackgroundTransparency = 1
	title.Font = Enum.Font.Gotham
	title.TextColor3 = Color3.fromRGB(255, 255, 255)
	title.TextSize = 18
	title.Parent = frame

	local close = Instance.new("TextButton")
	close.Text = "X"
	close.Size = UDim2.new(0, 25, 0, 25)
	close.Position = UDim2.new(1, -30, 0, 5)
	close.BackgroundTransparency = 1
	close.Font = Enum.Font.GothamBold
	close.TextColor3 = Color3.fromRGB(255, 80, 80)
	close.TextSize = 16
	close.Parent = frame
	close.MouseButton1Click:Connect(function() gui:Destroy() end)

	-- ✅ Editable TextBox (Fixed)
	-- ✅ Editable key box with placeholder only
local keyBox = Instance.new("TextBox")
keyBox.PlaceholderText = "Enter your key here..."
keyBox.Size = UDim2.new(0.9, 0, 0, 30)
keyBox.Position = UDim2.new(0.05, 0, 0.3, 0)
keyBox.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
keyBox.BorderColor3 = Color3.fromRGB(255, 255, 255)
keyBox.BorderSizePixel = 1
keyBox.TextColor3 = Color3.fromRGB(255, 255, 255)
keyBox.Font = Enum.Font.Gotham
keyBox.TextSize = 16
keyBox.ClearTextOnFocus = false
keyBox.Text = "" -- ← empty (no "Textbox" or default text)
keyBox.Parent = frame


	local getBtn = Instance.new("TextButton")
	getBtn.Text = "Get Key"
	getBtn.Size = UDim2.new(0.42, 0, 0, 30)
	getBtn.Position = UDim2.new(0.05, 0, 0.55, 0)
	getBtn.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
	getBtn.BorderColor3 = Color3.fromRGB(255, 255, 255)
	getBtn.BorderSizePixel = 1
	getBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
	getBtn.Font = Enum.Font.Gotham
	getBtn.TextSize = 14
	getBtn.Parent = frame

	local verifyBtn = Instance.new("TextButton")
	verifyBtn.Text = "Verify Key"
	verifyBtn.Size = UDim2.new(0.42, 0, 0, 30)
	verifyBtn.Position = UDim2.new(0.53, 0, 0.55, 0)
	verifyBtn.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
	verifyBtn.BorderColor3 = Color3.fromRGB(255, 255, 255)
	verifyBtn.BorderSizePixel = 1
	verifyBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
	verifyBtn.Font = Enum.Font.Gotham
	verifyBtn.TextSize = 14
	verifyBtn.Parent = frame

	local discordBtn = Instance.new("TextButton")
	discordBtn.Text = "Join Discord"
	discordBtn.Size = UDim2.new(0.9, 0, 0, 30)
	discordBtn.Position = UDim2.new(0.05, 0, 0.75, 0)
	discordBtn.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
	discordBtn.BorderColor3 = Color3.fromRGB(255, 255, 255)
	discordBtn.BorderSizePixel = 1
	discordBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
	discordBtn.Font = Enum.Font.Gotham
	discordBtn.TextSize = 14
	discordBtn.Parent = frame

	local infoLabel = Instance.new("TextLabel")
	infoLabel.Size = UDim2.new(0.9, 0, 0, 20)
	infoLabel.Position = UDim2.new(0.05, 0, 0.9, 0)
	infoLabel.BackgroundTransparency = 1
	infoLabel.Font = Enum.Font.Gotham
	infoLabel.TextColor3 = Color3.fromRGB(200, 200, 200)
	infoLabel.TextSize = 12
	infoLabel.Text = ""
	infoLabel.Parent = frame

	getBtn.MouseButton1Click:Connect(function()
		local ok, msg = copyLink()
		infoLabel.Text = msg
	end)

	verifyBtn.MouseButton1Click:Connect(function()
		local key = keyBox.Text
		if key == "" then
			infoLabel.Text = "Please enter your key."
			return
		end
		local ok, msg = verifyKey(key)
		infoLabel.Text = msg
		if ok then
			task.wait(1)
			onMessage("Access granted! Loading script...")
			loadstring(game:HttpGet("https://raw.githubusercontent.com/ZuriHubV2/xzcxzca/refs/heads/main/nfdndg"))()
			gui:Destroy()
		end
	end)

	discordBtn.MouseButton1Click:Connect(function()
		if fSetClipboard then
			fSetClipboard("https://discord.gg/mcZcNV9Sgg")
			infoLabel.Text = "Discord invite copied to clipboard!"
		else
			infoLabel.Text = "Join: https://discord.gg/mcZcNV9Sgg"
		end
	end)
end)
